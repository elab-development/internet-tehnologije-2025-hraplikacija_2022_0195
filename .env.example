# SMTP Configuration for Birthday Email Service
# Uncomment and fill in your SMTP server details

# SMTP Server Host (e.g., smtp.gmail.com, smtp.office365.com)
# SMTP_HOST=smtp.gmail.com

# SMTP Server Port (typically 587 for TLS, 465 for SSL)
# SMTP_PORT=587

# Use SSL/TLS (true for port 465, false for 587)
# SMTP_SECURE=false

# SMTP Authentication
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# From address for birthday emails (defaults to SMTP_USER if not set)
# SMTP_FROM=noreply@company.com

# Existing environment variables
DATABASE_URL=postgresql://user:password@localhost:5432/database

# Example Gmail setup:
# 1. Enable 2-Factor Authentication on your Google Account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated password as SMTP_PASSWORD
# 4. SMTP_HOST=smtp.gmail.com
# 5. SMTP_PORT=587
# 6. SMTP_SECURE=false
# 7. SMTP_USER=your-email@gmail.com

# Example Office 365 setup:
# 1. SMTP_HOST=smtp.office365.com
# 2. SMTP_PORT=587
# 3. SMTP_SECURE=false
# 4. SMTP_USER=your-email@company.com
# 5. SMTP_PASSWORD=your-office-password
